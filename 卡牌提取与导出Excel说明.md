# 杀戮尖塔2 言灵MOD 开发规划与进度记录

## 项目背景

基于敏捷开发（Agile）思路，分四个阶段实现"言灵"MOD——通过语音识别控制游戏打牌的核心功能。每个阶段都有独立的可交付产出，不依赖后续阶段。

---

## 四个阶段总览

| 阶段 | 目标 | 难度 | 状态 |
| --- | --- | --- | --- |
| 阶段一 | 生成所有卡牌的中/日/英三语 TTS 语音包 | 简单 | 已完成 ✅ |
| 阶段二 | 键盘输入触发游戏出牌并播放语音 | 中等 | 未开始 |
| 阶段三 | 接入本地语音识别，喊出卡牌名触发出牌 | 困难 | 未开始 |
| 阶段四 | 模糊语义解析，支持"打最右边那个"等指向性指令 | 极难 | 未开始 |

---

## 阶段一：TTS 音频资产生成

### 目标

批量生成所有卡牌的英语和日语配音，输出为 `.ogg` 格式音频文件。

### 已完成

#### 1. 数据提取（已完成 ✅）

已从游戏本地化文件中提取三语卡牌数据，导出为 `CARD_ZH.xlsx`。

**数据来源文件：**

- `CARD_ZH.json` → 中文本地化
- `CARD_ENG.json` → 英文本地化
- `CARD_JPN.json` → 日文本地化

**Excel 结构（已验证，共 606 张卡牌）：**

| 卡牌ID | 卡牌中文名称 | 卡牌日文名称 | 卡牌英文名称 | 描述 |
| --- | --- | --- | --- | --- |
| ABRASIVE | 磨蚀 | 刺々しい | Abrasive | 获得...敏捷...荆棘 |

**提取脚本：** `reserve.py`

**核心技术点：**

- 用 `json` 模块读取 JSON 文件
- 用 `endswith(".title")` 筛选卡牌名字键
- 用 `replace(".title", "")` 提取卡牌ID
- 用 `get(键名, "")` 安全取描述（避免无描述的卡牌报错）
- 用 `openpyxl` 写入 Excel

---

### 2. TTS 音频批量生成（进行中 🔄）

**技术选型决策：**

采用两阶段策略，先跑通流程，后替换模型：

| 阶段 | 工具 | 用途 |
| --- | --- | --- |
| 当前 MVP | `edge-tts` | 快速跑通流程，验证管道可行性 |
| 后续升级 | `VITS` | 声音克隆，模仿声优或游戏音效风格 |

**文件夹结构设计（按模型 → 语言分层）：**

```
audio/
  edge-tts/
    CN/    → 中文音频（zh-CN-XiaoxiaoNeural）
    ENG/   → 英文音频（en-US-AvaMultilingualNeural）
    JPN/   → 日文音频（ja-JP-NanamiNeural）
  vits/
    声线一/
      CN/
      ENG/
      JPN/
```

设计原则：按模型分第一层，按语言分第二层，将来换模型只加一个新文件夹，原有数据不受影响（数据解耦）。

**文件命名规范：**

格式为 `卡牌ID_语言.mp3`，例如：
- `ABRASIVE_CN.mp3`
- `ABRASIVE_ENG.mp3`
- `ABRASIVE_JPN.mp3`

保留语言后缀的原因：文件移动后也能识别语言归属，可移植性更强。

**生成脚本：** `TTS.py`

**核心技术点：**

- `asyncio` + `async def`：处理 Edge-TTS 的异步网络请求
- `openpyxl.load_workbook()`：从已有 Excel 读取数据（区别于 `Workbook()` 新建）
- `iter_rows(min_row=2, values_only=True)`：遍历 Excel 数据行，跳过表头
- `f-string`：更简洁的字符串拼接方式，如 `f"路径/{CARD_ID}_ENG.mp3"`
- `os.makedirs(路径, exist_ok=True)`：自动创建多层文件夹，已存在不报错

**当前进度：**

- [x] 选定 TTS 工具：`edge-tts`
- [x] 设计文件夹结构
- [x] 完成 `TTS.py` 脚本编写
- [x] 批量生成 606 × 3 = 1818 个音频文件
- [x] 验证生成数量完整（ENG: 606 / JPN: 606 / CN: 606）
- [x] 音频格式转码：`.mp3` → `.ogg`（转码脚本：`translator.py`）

**音频格式说明：**

Edge-TTS 输出 `.mp3`，但 Godot 引擎最推荐 `.ogg`（体积更小、原生支持更好、无专利限制）。

转码工具：`ffmpeg`，生成完成后批量转码。

---

## 阶段二：游戏逻辑接入（未开始）

### 目标

不用麦克风，通过键盘输入特定指令，游戏自动打出对应卡牌并播放阶段一生成的语音。

### 语言选型

选用 **GDScript**，理由：

| 对比项 | GDScript | C# |
| --- | --- | --- |
| 上手难度 | 低，语法简单，接近 Python | 较高，需要了解静态类型系统 |
| 游戏更新后修改 | 容易，脚本文件独立，改动局限性小 | 较麻烦，编译环境依赖更多 |
| Godot 原生支持 | 完全原生，文档最完善 | 需要额外配置 .NET 环境 |
| 适合当前阶段 | ✅ 最适合 | 后期复杂逻辑再考虑 |

结论：**先用 GDScript 跑通逻辑，后续如有性能或复杂度需求再考虑 C#。**

### 需要学习

- 安装 Godot 引擎（当前尚未安装）
- GDScript 基础语法
- 如何通过代码获取当前手牌列表对象
- 如何调用卡牌的 `Play()` 方法
- 如何触发 Audio 节点播放 ogg 音频

### 下一步行动

- [ ] 安装 Godot 引擎
- [ ] 完成 GDScript 基础入门
- [ ] 研究杀戮尖塔2 的 Mod 接口

---

## 阶段三：语音识别接入（未开始）

### 目标

对着麦克风喊出卡牌名，游戏做出反应。

### 需要学习

- 本地离线 ASR 模型部署（Vosk 或轻量 Whisper）
- 后台监听进程的实现
- 进程间通信（IPC / Socket）将指令发送给 Godot Mod

---

## 阶段四：模糊语义解析（未开始）

### 目标

支持自然语言指向性指令，例如"痛击最右边那个"。

### 需要学习

- 意图识别（Intent）与实体提取（Entity）
- 游戏场景中怪物坐标的空间建模
- 自然语言到游戏状态的映射逻辑

---

## 开发环境

| 设备 | 用途 |
| --- | --- |
| Lenovo R9000p | 主力开发机，运行 Godot、跑本地代码、Hook 测试 |
| Mac mini | 辅助节点，音频转码、轻量脚本、跨平台测试 |

- 开发语言：Python（数据处理）、GDScript / C#（游戏 Mod）
- 虚拟环境：`V1`（位于 `D:\VsProject\V1`）
- 已安装库：`openpyxl`、`edge-tts`、`asyncio`（内置）、`json`（内置）、`os`（内置）、`subprocess`（内置）、`sys`（内置）

---

## 阶段一产出文件清单

| 文件 | 用途 |
| --- | --- |
| `reserve.py` | 从 JSON 提取卡牌数据，导出为 Excel |
| `CARD_ZH.xlsx` | 三语卡牌数据表（606 张） |
| `TTS.py` | 调用 edge-tts 批量生成 mp3 音频 |
| `translator.py` | 调用 ffmpeg 批量转码 mp3 → ogg |
| `audio/edge-tts/` | 原始 mp3 音频（1818 个） |
| `audio/edge-tts-ogg/` | 转码后 ogg 音频（1818 个），供 Godot 使用 |
